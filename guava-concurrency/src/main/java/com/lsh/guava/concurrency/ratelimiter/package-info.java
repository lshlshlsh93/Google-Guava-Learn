/**
 * @Author lishaohui
 * @Date 2023/5/24 23:38
 * <div>
 * <p> the rate limiter implement of the semaphore is at {@link com.lsh.guava.concurrency.ratelimiter.semaphore.SemaphoreDemo} </p>
 * <p> the rate limiter implement of the semaphore is at {@link com.lsh.guava.concurrency.ratelimiter.guava_ratelimiter.RateLimiterDemo} </p>
 *
 * <p> the rate limiter implement of the leaky bucket is at {@link com.lsh.guava.concurrency.ratelimiter.leaky_bucket.LeakyBucket}</p>
 * <p> the rate limiter implement of the leaky bucket test is at {@link com.lsh.guava.concurrency.ratelimiter.leaky_bucket.LeakyBucketClient}</p>
 *
 * <p> the rate limiter implement of the token bucket is at {@link com.lsh.guava.concurrency.ratelimiter.token_bucket.TokenBucket}</p>
 * <p> the rate limiter implement of the token bucket test is at {@link com.lsh.guava.concurrency.ratelimiter.token_bucket.TokenBucketClient} </p>
 * </div>
 */
package com.lsh.guava.concurrency.ratelimiter;